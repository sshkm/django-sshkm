ALLOWED_HOSTS = ['*']

CELERY_BROKER_URL = 'amqp://'
CELERY_RESULT_BACKEND = 'rpc'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join('/etc/sshkm/', 'db.sqlite3'),
    }
}
